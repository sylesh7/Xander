// Minimal first-paint placeholder. The original prerendered snapshot here was
// scraped from the template's old product and carried its old branding in the
// markup text; the Vite bundle replaces #root's contents on mount anyway
// (createRoot, not hydrate), so this only needs to hold the seat for the
// fraction of a second before that happens.
export const ROOT_HTML =
  '<main style="display:flex;min-height:100vh;align-items:center;justify-content:center;font-family:system-ui,sans-serif;color:#888;">Loading Xander…</main>';
